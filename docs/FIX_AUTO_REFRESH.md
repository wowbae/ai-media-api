# Исправление автообновления интерфейса чата

## Проблема

1. **Фронтенд не обновлял интерфейс автоматически**: После отправки запроса на генерацию медиа пользователь не видел новые сообщения и фото в чате, пока не обновит страницу вручную (F5).

2. **Отсутствие инвалидации при фокусе**: Когда пользователь возвращался на вкладку с чатом после переключения на другую вкладку, данные оставались устаревшими.

## Решение

### 1. Включение автообновления при фокусе окна

**Файл**: `ai-media-api/src/redux/api/base.ts`

```typescript
export const baseApi = createApi({
  reducerPath: "mediaApi",
  baseQuery: fetchBaseQuery({
    baseUrl: API_BASE_URL,
  }),
  tagTypes: ["Chat", "Request", "File", "Model"],
  keepUnusedDataFor: 60,
  refetchOnMountOrArgChange: 10,
  refetchOnFocus: true, // ✅ Было: false → Стало: true
  refetchOnReconnect: true,
  endpoints: () => ({}),
});
```

**Что изменилось**:
- `refetchOnFocus: false` → `refetchOnFocus: true`

**Результат**: RTK Query теперь автоматически обновляет все активные запросы при возвращении фокуса на окно браузера.

---

### 2. Добавление инвалидации данных при фокусе на странице чата

**Файл**: `ai-media-api/src/routes/media/$chatId.tsx`

Добавлен новый `useEffect` для явного обновления данных при фокусе:

```typescript
// Инвалидация данных при фокусе на окне браузера
// Это гарантирует, что пользователь всегда видит актуальные данные
// когда возвращается на вкладку с чатом
useEffect(() => {
  const handleFocus = () => {
    console.log("[Chat] 🔄 Окно получило фокус, обновляем данные чата");

    // Обновляем данные чата
    Promise.all([
      refetch(),
      !shouldSkipFullLoad ? refetchFull() : Promise.resolve(),
    ]).catch((error) => {
      console.error("[Chat] Ошибка при обновлении чата после фокуса:", error);
    });
  };

  // Подписываемся на событие фокуса окна
  window.addEventListener("focus", handleFocus);

  // Отписываемся при размонтировании
  return () => {
    window.removeEventListener("focus", handleFocus);
  };
}, [refetch, refetchFull, shouldSkipFullLoad]);
```

**Что изменилось**:
- Добавлен слушатель события `window.focus`
- При получении фокуса окном автоматически вызываются `refetch()` и `refetchFull()`
- Обновляются как частичные данные (последние 3 запроса), так и полные данные чата

**Результат**: Данные чата обновляются моментально при возврате пользователя на вкладку.

---

### 3. Улучшение инвалидации тегов после генерации

**Файл**: `ai-media-api/src/redux/api/media.endpoints.ts`

Расширен список инвалидируемых тегов в мутации `generateMedia`:

```typescript
generateMedia: build.mutation<GenerateMediaResponse, GenerateMediaRequest>({
  // ... query и transformResponse
  
  // Инвалидируем кеш чата при успехе, чтобы немедленно обновить UI
  invalidatesTags: (result, _error, { chatId }) => [
    { type: "Chat", id: chatId },           // ✅ Обновляем конкретный чат
    { type: "Chat", id: "LIST" },           // ✅ Обновляем список всех чатов (счетчики)
    { type: "Request", id: result?.requestId || "LIST" }, // ✅ Обновляем конкретный запрос
    { type: "Request", id: "LIST" },        // ✅ Обновляем список всех запросов
  ],
}),
```

**Что изменилось**:
- Добавлена инвалидация `{ type: "Chat", id: "LIST" }` — обновляет список всех чатов и счетчики
- Добавлена инвалидация `{ type: "Request", id: "LIST" }` — обновляет список всех запросов

**Результат**: После отправки запроса на генерацию RTK Query автоматически обновляет все связанные данные.

---

## Как это работает

### Поток обновления данных:

1. **Пользователь отправляет запрос на генерацию**
   - Вызывается мутация `generateMedia`
   - Создается новый запрос на сервере

2. **Немедленное обновление UI** (за счет invalidatesTags)
   - RTK Query инвалидирует теги `Chat`, `Request`
   - Автоматически перезапрашиваются данные чата
   - Новый запрос появляется в списке с статусом `PENDING`

3. **Polling для отслеживания статуса**
   - Компонент запускает polling через `useGetRequestQuery`
   - Статус обновляется: `PENDING` → `PROCESSING` → `COMPLETED`/`FAILED`

4. **Обновление при возврате фокуса**
   - Если пользователь переключился на другую вкладку
   - При возврате срабатывает событие `focus`
   - Данные автоматически обновляются через `refetch()`

### Преимущества решения:

✅ **Мгновенное отображение**: Новые запросы появляются сразу после отправки  
✅ **Актуальные данные**: При возврате на вкладку данные всегда свежие  
✅ **Оптимальная производительность**: Обновления происходят только при необходимости  
✅ **Надежность**: Несколько уровней обновления гарантируют получение данных  

---

## Тестирование

### Сценарий 1: Отправка запроса на генерацию

1. Откройте чат
2. Отправьте запрос на генерацию (например, "красивый закат")
3. ✅ **Ожидаемый результат**: Запрос сразу появляется в чате со статусом `PENDING`
4. ✅ **Ожидаемый результат**: После завершения генерации файлы отображаются автоматически

### Сценарий 2: Возврат на вкладку

1. Откройте чат
2. Отправьте запрос на генерацию
3. Переключитесь на другую вкладку браузера
4. Подождите завершения генерации (30-60 секунд)
5. Вернитесь на вкладку с чатом
6. ✅ **Ожидаемый результат**: Данные автоматически обновляются, файлы отображаются

### Сценарий 3: Работа с несколькими чатами

1. Откройте несколько чатов в разных вкладках
2. Отправьте запросы в разных чатах
3. Переключайтесь между вкладками
4. ✅ **Ожидаемый результат**: В каждом чате данные актуальны при возврате фокуса

---

## Технические детали

### RTK Query Tag System

RTK Query использует систему тегов для управления кешем:

- **`Chat`** — данные конкретного чата
  - `{ type: "Chat", id: 123 }` — чат с ID 123
  - `{ type: "Chat", id: "LIST" }` — список всех чатов

- **`Request`** — данные запросов на генерацию
  - `{ type: "Request", id: 456 }` — запрос с ID 456
  - `{ type: "Request", id: "LIST" }` — список всех запросов

- **`File`** — данные файлов
  - `{ type: "File", id: 789 }` — файл с ID 789
  - `{ type: "File", id: "LIST" }` — список всех файлов

Когда мутация инвалидирует тег, все запросы с этим тегом автоматически перезапрашиваются.

### Window Focus API

Используется стандартный Web API:

```typescript
window.addEventListener("focus", handleFocus);
window.removeEventListener("focus", handleFocus);
```

- Срабатывает при возврате фокуса на окно браузера
- Поддерживается всеми современными браузерами
- Не требует дополнительных библиотек

---

## Отладка

### Логи в консоли

Для отладки добавлены подробные логи:

```javascript
// При отправке запроса
[RTK Query] generateMedia mutation вызван: { chatId, prompt, model, ... }

// При получении ответа
[RTK Query] generateMedia response получен: { requestId, status, ... }

// При возврате фокуса
[Chat] 🔄 Окно получило фокус, обновляем данные чата

// При polling
[RTK Query] getRequest response: { id, status, filesCount, ... }
```

### Проверка инвалидации

В Redux DevTools можно отследить:
1. Вызов мутации `generateMedia`
2. Инвалидацию тегов `Chat` и `Request`
3. Автоматический refetch запросов `getChat` и `getRequest`

---

## Связанные файлы

- `ai-media-api/src/redux/api/base.ts` — базовая конфигурация RTK Query
- `ai-media-api/src/redux/api/media.endpoints.ts` — эндпоинты API
- `ai-media-api/src/routes/media/$chatId.tsx` — страница чата
- `ai-media-api/docs/NANO_BANANA_PRO_KIEAI.md` — документация по модели

---

## Дата изменений

**Дата**: 2024-01-XX  
**Версия**: 1.0.0  
**Автор**: AI Assistant  
**Статус**: ✅ Реализовано и протестировано